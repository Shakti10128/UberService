spring:
  application:
    name: Uber-BookingService
  datasource:
    url: jdbc:mysql://localhost:3306/uber_db
    password: sh@kti12345
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

server:
  port: 8084

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka #Eureka service url

  instance:
    prefer-ip-address: true

