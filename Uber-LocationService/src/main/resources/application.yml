spring:
  application:
    name: Uber-LocationService
  data:
    redis:
      host: localhost
      port: 6379
server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka #Eureka service url

  instance:
    prefer-ip-address: true
