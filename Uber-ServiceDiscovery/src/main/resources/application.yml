spring:
  application:
    name: Uber-ServiceDiscovery

server:
  port: 8761

eureka:
  instance:
    hostname: my-registry
  client:
    register-with-eureka: false
    fetch-registry: false
#    service-url:
#      defaultZone: